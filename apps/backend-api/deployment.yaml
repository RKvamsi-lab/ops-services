apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend-api
  namespace: backend-api

spec:
  replicas: 2

  selector:
    matchLabels:
      app: backend-api

  template:
    metadata:
      labels:
        app: backend-api

    spec:
      containers:
        - name: backend-api
          image: 719572784863.dkr.ecr.us-east-1.amazonaws.com/backend-api:978415faa5c89b3b513d0343250ecb2232cbb5a3
          imagePullPolicy: Always

          securityContext:
            privileged: false

          env:
            - name: NODE_ENV
              value: "production"

            - name: PORT
              value: "8080"

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: backend-api-secret
                  key: DATABASE_URL

          ports:
            - containerPort: 8080

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          readinessProbe:
            httpGet:
              path: /api/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20